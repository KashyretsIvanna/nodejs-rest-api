const router = require("express").Router();
const fs = require("fs");

// router.get("/avatars/:photo", (req, res) => {
//   const fileName = req.params.photo;
//   res.send(fs.readFile("../../public/fon.jpg"));

//   res.writeHead(200, { "Content-Type": "image/jpg" });
// });

module.exports = router;
